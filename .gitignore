# Python
__pycache__/
*.pyc
.venv/
*.egg-info/
.pytest_cache/

# Model weights (fetched separately, see README)
*.pt

# Go -- `go build` in the repo root drops a binary named after the module
/CT-Flow
/api

# Web
frontend/node_modules/
frontend/.next/
# tsc --incremental's cache. Machine-local build output with no review
# value, and it was tracked -- so every branch that ran a type-check
# carried a one-line diff of it.
frontend/tsconfig.tsbuildinfo

# Media / large assets
*.mp4
*.mov
*.MOV

# Local runtime data
backend/_smoke_out/
backend/_smoke_test_set/
backend/_smoke_upload/
backend/_experiment_out/

# Docker / secrets
.env
certs/*.crt

# OS
Thumbs.db
.DS_Store
